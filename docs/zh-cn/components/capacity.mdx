import { CodeShow, Attributes } from '../../../pages'

# Capacity / 容量

显示一个容量指示器

<CodeShow title="默认的"  code={`<template>
 <div>
   <fect-capacity value="30"/>
   <fect-spacer y={.5}/>
   <fect-capacity value="60"/>
   <fect-spacer y={.5}/>
   <fect-capacity value="80"/>
 </div>
</template>
 
<script>
   
export default{
 
}
 
</script>
`}>

 <div>
   <fect-capacity value="30"/>
   <fect-spacer y={.5}/>
   <fect-capacity value="60"/>
   <fect-spacer y={.5}/>
   <fect-capacity value="80"/>
 </div>
</CodeShow>

<CodeShow title="固定颜色" desc="指定一个默认颜色进行覆盖"  code={`<template>
 <div>
   <fect-capacity value="30"/>
 </div>
</template>
 
<script>
   
export default{
 
}
 
</script>
`}>

 <div>
   <fect-capacity value="30" color="green"/>
 </div>
</CodeShow>

<fect-Attributes>

<fect-AttributesTitle title="Capacity Props" />

| 属性      | 描述                | 类型                | 可选值             | 默认 |
| --------- | ------------------- | ------------------- | ------------------ | ---- |
| **value** | 当前的容量值        | `'string','number'` | `-`                | `0`  |
| **color** | 自定义容量 css 颜色 | `string`            | `-`                | `-`  |
| ...       | 原生属性            | `HTMLAttributes`    | `'class','id',...` | `-`  |

</fect-Attributes>
